# RuleKit comparison study dependencies.
#
# Install:
#   pip install -r requirements.txt
#
# Or with a virtual environment (recommended):
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# Anthropic SDK — for LLM calls (RuleKit Map + Direct-LLM baseline).
# Loaded lazily; not required for offline-only runs (smoke tests etc.).
anthropic>=0.40.0

# PyYAML — for policy config, case files, build specs.
PyYAML>=6.0

# That's it. The rest is Python stdlib:
#   argparse, csv, dataclasses, glob, json, math, os, pickle, platform,
#   re, socket, subprocess, sys, time, abc, collections, enum, itertools,
#   typing
